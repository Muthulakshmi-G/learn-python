#class and instance variables
class Dog:

    kind='canine' #this is  class variable shared by all instances

    def __init_(self,name,):
        self.name=name #instance variable uniqe to each instance

    d=Dog('fido')
    m=Dog('Buddy')

d.kind)
m.kind
d.name
m.name
